version: '3.8'

services:
  garmin-dashboard:
    image: ghcr.io/rpetersson/garmin-r10-dashboard:latest
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
